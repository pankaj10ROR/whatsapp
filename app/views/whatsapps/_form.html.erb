<%= form_with(model: whatsapp, local: true) do |form| %>
  <% if whatsapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(whatsapp.errors.count, "error") %> prohibited this whatsapp from being saved:</h2>

      <ul>
      <% whatsapp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
